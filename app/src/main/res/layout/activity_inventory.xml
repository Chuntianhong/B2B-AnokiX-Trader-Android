<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        style="@style/Widget.AnokiX.Toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:navigationIcon="?attr/homeAsUpIndicator"
        app:title="@string/nav_inventory"
        app:titleTextColor="#FFFFFF" />

    <!-- Stat cards (mirrors portal: Total Stock Value / Total Units / Low Stock / Out of Stock) -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:overScrollMode="never"
        android:paddingHorizontal="12dp"
        android:paddingTop="12dp"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <com.google.android.material.card.MaterialCardView style="@style/InvStatCard">
                <LinearLayout style="@style/InvStatCardInner">
                    <com.anokix.trader.ui.views.RobotoTextView style="@style/InvStatCardLabel" android:text="Total Stock Value" />
                    <com.anokix.trader.ui.views.RobotoBoldTextView android:id="@+id/statValue" style="@style/InvStatCardValue" android:textColor="@color/text_primary" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <com.google.android.material.card.MaterialCardView style="@style/InvStatCard">
                <LinearLayout style="@style/InvStatCardInner">
                    <com.anokix.trader.ui.views.RobotoTextView style="@style/InvStatCardLabel" android:text="Total Units in Stock" />
                    <com.anokix.trader.ui.views.RobotoBoldTextView android:id="@+id/statUnits" style="@style/InvStatCardValue" android:textColor="@color/text_primary" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <com.google.android.material.card.MaterialCardView style="@style/InvStatCard">
                <LinearLayout style="@style/InvStatCardInner">
                    <com.anokix.trader.ui.views.RobotoTextView style="@style/InvStatCardLabel" android:text="Low Stock Items" />
                    <com.anokix.trader.ui.views.RobotoBoldTextView android:id="@+id/statLow" style="@style/InvStatCardValue" android:textColor="@color/warning" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <com.google.android.material.card.MaterialCardView style="@style/InvStatCard">
                <LinearLayout style="@style/InvStatCardInner">
                    <com.anokix.trader.ui.views.RobotoTextView style="@style/InvStatCardLabel" android:text="Out of Stock Items" />
                    <com.anokix.trader.ui.views.RobotoBoldTextView android:id="@+id/statOut" style="@style/InvStatCardValue" android:textColor="@color/danger" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Filter chips (portal stock-movement tabs) -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:overScrollMode="never"
        android:paddingVertical="10dp"
        android:scrollbars="none">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/filterChips"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            app:singleLine="true"
            app:singleSelection="true" />
    </HorizontalScrollView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/inventoryList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingHorizontal="12dp"
        android:paddingBottom="16dp" />
</LinearLayout>
