<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Temporary location for camera captures (logo / document photos). -->
    <cache-path
        name="captures"
        path="captures/" />
    <external-cache-path
        name="external_captures"
        path="captures/" />
    <!-- Decoded GRV PDFs shared to a viewer via FileProvider. -->
    <cache-path
        name="grv_pdfs"
        path="grv/" />
    <!-- Downloaded report files (CSV) opened or shared via FileProvider. -->
    <external-files-path
        name="report_downloads"
        path="Download/" />
    <files-path
        name="report_downloads_internal"
        path="downloads/" />
</paths>
