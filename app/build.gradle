plugins {
    id 'com.android.application'
    // Reads google-services.json and generates Firebase config resources.
    id 'com.google.gms.google-services'
}

android {
    namespace 'com.anokix.traderapp'
    compileSdk 34

    defaultConfig {
        applicationId "com.anokix.traderapp"
        minSdk 24
        targetSdk 34
        versionCode 107
        versionName "1.0.7"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'androidx.recyclerview:recyclerview:1.3.2'
    implementation 'androidx.swiperefreshlayout:swiperefreshlayout:1.1.0'
    implementation 'androidx.cardview:cardview:1.0.0'
    implementation 'androidx.viewpager2:viewpager2:1.0.0'

    // Chrome Custom Tabs — the PayCloud top-up checkout opens here rather than in a
    // WebView, so the trader can see the real address bar and padlock on the page they
    // are typing card details into, and their browser's autofill / 3-D Secure works.
    implementation 'androidx.browser:browser:1.7.0'

    // Networking: OkHttp transport + Gson for JSON parsing (no Retrofit)
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation 'com.squareup.okhttp3:logging-interceptor:4.12.0'
    implementation 'com.google.code.gson:gson:2.10.1'

    // Image loading + caching
    implementation 'com.github.bumptech.glide:glide:4.16.0'

    // POS Scan / QR: CameraX drives the preview + frame analysis, ML Kit decodes.
    // The *bundled* barcode model (not the Play-Services-hosted one) is deliberate —
    // a till has to scan the moment it opens, with no first-run model download.
    implementation 'androidx.camera:camera-core:1.3.4'
    implementation 'androidx.camera:camera-camera2:1.3.4'
    implementation 'androidx.camera:camera-lifecycle:1.3.4'
    implementation 'androidx.camera:camera-view:1.3.4'
    implementation 'com.google.mlkit:barcode-scanning:17.3.0'

    // Banner / promotion video streaming
    implementation 'androidx.media3:media3-exoplayer:1.4.1'
    implementation 'androidx.media3:media3-ui:1.4.1'

    // Google Maps (store geolocation picker)
    implementation 'com.google.android.gms:play-services-maps:18.2.0'

    // Country code picker (phone country selector with flags + search)
    implementation 'com.hbb20:ccp:2.7.3'

    // Firebase Cloud Messaging (push notifications). The BoM pins compatible
    // versions so firebase-messaging needs no explicit version.
    implementation platform('com.google.firebase:firebase-bom:33.7.0')
    implementation 'com.google.firebase:firebase-messaging'
}
